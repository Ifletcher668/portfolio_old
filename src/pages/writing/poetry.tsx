import React from 'react';
import Layout from '../../components/layout';

const PoetryPage: React.FC<IProps> = (props: IProps) => {
    return (
        <Layout>
            <h1>Poetry</h1>
        </Layout>
    );
};

export default PoetryPage;
